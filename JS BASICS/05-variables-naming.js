let _name = 'John';
let $balance= 100;

// Can we start variable names with digits ? NO can be used anywhere but not in the beginning

var score1 = 1;
var score11 = 11;

// Can we have specials other than _name, $ and other currency signs within the variable? no
let age = 25;

let Age = 45; // not preferred

console.log(age); // 25
console.log(Age); // 45

// can we have spaces in the variable name? NO we use camel case rules for this
const firstMonthOfTheYear = 'January';
